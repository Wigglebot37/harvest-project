/// @description Enables View
// Turns on views and allows view 0 to be used
view_enabled=true;
view_visible[0]=true;

// Goes to a starting room after initializing objects for a frame
if(room==rm_0) room_goto(rm_start);